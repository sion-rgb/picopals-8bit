import {useEffect,useRef} from "react";
import {bySpecies,npcs} from "../data";
import {drawNpcSprite,drawSpeciesSprite} from "../sprites";
import type {NpcStage} from "../types";
export function SpeciesPreviewCanvas({speciesId,silhouette=false,size=88}:{speciesId:string;silhouette?:boolean;size?:number}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current?.getContext("2d");if(!c)return;c.imageSmoothingEnabled=false;c.clearRect(0,0,88,88);drawSpeciesSprite(c,bySpecies(speciesId),{x:44,y:51},1.35,"idle",{silhouette,frame:1})},[speciesId,silhouette]);return <canvas ref={ref} width="88" height="88" style={{width:size,height:size}} className="species-preview" data-sprite-id={speciesId} aria-label={silhouette?"未解鎖角色剪影":bySpecies(speciesId).name}/>}
export function NpcPreviewCanvas({npcId,stage,size=180,animation="idle"}:{npcId:string;stage:NpcStage;size?:number;animation?:string}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current?.getContext("2d"),npc=npcs.find(n=>n.id===npcId);if(!c||!npc)return;let frame=0,raf=0,last=0;const draw=(t:number)=>{if(t-last>300){frame++;last=t}c.clearRect(0,0,100,100);c.fillStyle="#fff8ef";c.fillRect(0,0,100,100);drawNpcSprite(c,npc.looks[stage],{x:50,y:60},1.5,animation,frame);raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw);return()=>cancelAnimationFrame(raf)},[npcId,stage,animation]);return <canvas ref={ref} width="100" height="100" style={{width:size,height:size}} className="npc-preview" data-npc={`${npcId}-${stage}`} aria-label={`${npcs.find(n=>n.id===npcId)?.name}${stage}造型`}/>}
