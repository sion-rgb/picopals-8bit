import {expect,test} from "@playwright/test";
test.beforeEach(async({page})=>{await page.goto("/");await page.getByRole("button",{name:/開始孵蛋/}).click()});
test("首次孵蛋、餵食、清潔與存檔保留",async({page})=>{await expect(page.getByText("正在等你")).toBeVisible();await page.getByRole("button",{name:/餵飼/}).first().click();await page.getByRole("button",{name:/餵飼 · 還有/}).first().click();await expect(page.getByText(/吃得很開心/)).toBeVisible();await page.getByRole("button",{name:/清潔/}).first().click();await page.getByRole("button",{name:/小掃帚/}).click();await page.reload();await expect(page.getByText(/正在等你/)).toBeVisible()});
test("三款小遊戲可開啟",async({page})=>{await page.getByRole("button",{name:/遊玩/}).first().click();for(const name of ["接住愛心","星星記憶牌","跳跳緞帶"]){await page.getByRole("button",{name:new RegExp(name)}).click();await expect(page.getByRole("heading",{name})).toBeVisible();await page.getByRole("button",{name:"返回"}).click()}});
test("匯出存檔",async({page})=>{await page.getByRole("button",{name:/設定/}).first().click();const download=page.waitForEvent("download");await page.getByRole("button",{name:"匯出 JSON"}).click();expect((await download).suggestedFilename()).toContain("picopals-save")});
test("畫面沒有橫向溢出",async({page})=>{expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true)});
test("測試模式可完成結婚流程",async({page})=>{await page.evaluate(()=>indexedDB.deleteDatabase("PicoPalsDB"));await page.goto("/?testMode=1");await page.getByRole("button",{name:/開始孵蛋/}).click();await page.getByRole("button",{name:/社交/}).first().click();await page.getByRole("button",{name:/求婚/}).click();await expect(page.getByText(/結為伴侶/)).toBeVisible()});
